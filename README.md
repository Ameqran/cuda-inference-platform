# CUDA Inference Platform

Production-grade asynchronous GPU inference platform organized as a strict DDD multi-module Maven project:

- `inference-domain`
- `inference-application`
- `inference-infrastructure`
- `inference-interfaces`
- `inference-boot`

The implementation follows the project prompt in `project-initial-prompt.md` with iterative delivery and commits.
